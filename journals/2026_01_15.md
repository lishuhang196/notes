- 项目工程 #前端知识点
	- 项目结构应该如何组织？
		- monorepo
		- multirepo
	- 如何规范项目包管理工具使用？
	- 项目代码规范组织？
	- `pnpm workspace` https://juejin.cn/post/7098609682519949325
- 应该如何规划自己的任务
- 前端读取并解析`xlsx\csv`文件
	- 如何判断文件字符串内容编码
	  logseq.order-list-type:: number
		- `xlsx`文件内部使用`utf-8`编码，`csv`文件需要注意当前的
		  logseq.order-list-type:: number
		- `utf-8`和`utf-8 with BOM`有什么区别 #编码
		  logseq.order-list-type:: number
	- 如何使用JS检测当前文件使用的编码格式
	  logseq.order-list-type:: number
		- logseq.order-list-type:: number
	- 前端文件读取模板
	  logseq.order-list-type:: number
	  
	  ```javascript
	  const fileInput = document.querySelector('#fileInput')
	  fileInput.addEventListener('change', () => {
	    const file = fileInput.files?.[0]
	    if (!file) return
	    const reader = new FileReader();
	    reader.onload = () => {
	      // 读取结果
	      console.log(reader.result);
	    }
	    // 读取文件
	    reader.readAsArrayBuffer(file)
	    reader.readAsText(file)
	  })
	  ```