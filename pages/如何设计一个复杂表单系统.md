- 第一步：确认需求
	- 字段多（上百个字段）
	- 动态联动表单校验
- 第二步：设计方案
	- 方案一（适用于表单变化不大的场景）：
	  collapsed:: true
		- 表单校验（以`el-form`为例）：
		  collapsed:: true
			- `rules`属性：表单rules配置通过`computed`进行统一配置，避免`rules`散落不同地方。
			- `validate-on-rule-change`属性：避免`rules`属性变化后立即触发。
		- 表单拆分：将页面中的字段进行拆分成组件，每个组件独立复杂表单内容和校验，最后由父组件统一收集和校验子组件数据并提交。子组件统一提供`validate`方法。
		- 兄弟表单组件数据通信：
		  collapsed:: true
			- vuex（耦合性较高）
			  logseq.order-list-type:: number
			- provider/inject + hooks方案
			  logseq.order-list-type:: number
	- 方案二（基于JSON Schema配置方案，适用于表单变化场景）：
		- [面向复杂场景的表单解决方案1. 背景 云音乐 B 端业务场景非常多，B 端业务相对于 C 端业务产品生命周期更长而且更注 - 掘金](https://juejin.cn/post/6862869137710907405?searchId=20251230163440350683454B52A605A7C1)
-