- 第一步：确认需求
	- 字段多（上百个字段）
	- 动态联动表单校验
- 第二步：设计方案
	- 表单校验（以`el-form`为例）：
		- `rules`属性：表单rules配置通过`computed`进行统一配置，避免`rules`散落不同地方。
		- `validate-on-rule-change`属性：避免`rules`属性变化后立即触发。
	- 表单拆分：将页面中的字段进行拆分成组件，每个组件独立复杂表单内容和校验，最后由父组件统一收集和校验子组件数据并提交。
	-