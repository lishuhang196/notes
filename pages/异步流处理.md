- ((696e024d-839e-4e36-840f-79d20a82377e))
- Node流，用于处理流式数据（文件、网络、内容）的读取、写入和转换。
- Node基础流类型：
	- `Readable Stream` 可读流
	- `Writable Stream` 可写流
	- `Transform Stream` 数据转换流，用于数据转换
	- `Duplex` 双向流，拥有独立的可读、可写端
-