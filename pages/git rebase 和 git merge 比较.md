- `git merge`
	- `git merge --ff-only` Git合并策略，**只允许进行快进（Fast-forward）合并**。
	  条件：当要合并的分支是当前分支的**直接子分支，且当前分支自此分支创建后没有新的提交时**。
		- 成功：Git 仅仅是将当前分支的指针快速移动到目标分支的最新提交上，保持历史记录干净。
		- 失败：条件不满足，显示报错
	- `git merge -ff` 默认，优先快进，快进不了就使用第三方合并。
- `git rebase`
-